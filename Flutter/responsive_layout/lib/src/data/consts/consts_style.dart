import 'package:flutter/widgets.dart';

abstract class AppRad {
  static final BorderRadius radius0 = BorderRadius.circular(4);
  static final BorderRadius radius1 = BorderRadius.circular(8);
  static final BorderRadius radius2 = BorderRadius.circular(16);
  static final BorderRadius radius3 = BorderRadius.circular(24);
}

abstract class AppPad {
  static final EdgeInsets padding0 = EdgeInsets.all(4);
  static final EdgeInsets padding1 = EdgeInsets.all(8);
  static final EdgeInsets padding2 = EdgeInsets.all(16);
  static final EdgeInsets padding3 = EdgeInsets.all(24);
}
